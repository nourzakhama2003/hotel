# ==========================================
# MAIN APPLICATION CONFIGURATION
# ==========================================
spring.application.name=hotel

# Active Profile - Set to 'dev' for local development
spring.profiles.active=dev

# Server Port
server.port=8082

#unknoun field in request dto exception
spring.jackson.deserialization.fail-on-unknown-properties=true

#base de donne
spring.datasource.url:jdbc:mysql://localhost:3306/hotel
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:}


#create tables if not exist
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true 

#protect api validate token access
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8080/realms/hotelrealm

# Keycloak Configuration for Face Authentication Token Generation
keycloak.auth-server-url=http://localhost:8080
keycloak.realm=hotelrealm
keycloak.resource=hotelclient
# Confidential client secret should come from the local environment
keycloak.credentials.secret=${KEYCLOAK_CLIENT_SECRET:}

# Keycloak Admin Configuration for bidirectional sync
keycloak.admin.server-url=http://localhost:8080
keycloak.admin.realm=master
keycloak.admin.client-id=admin-cli
keycloak.admin.username=admin
keycloak.admin.password=admin
keycloak.admin.target-realm=hotelrealm



##STRIPE
stripe.api.public.key=${STRIPE_PUBLIC_KEY:pk_test_51S1GWi4BxFUD5s92q3WSd0x9yKDzJtTTR4Vc2orIISwUOV97wVBhn6elbNRM7Vs1MNqrAlO7J12ZQ2xtHk32bqWn00mt8IdCtC}
stripe.api.secret.key=${STRIPE_SECRET_KEY:}


spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Face Recognition Configuration
face.recognition.threshold=0.85
face.recognition.upload.dir=${FACE_RECOGNITION_UPLOAD_DIR:./face-uploads}
face.recognition.service.url=${FACE_RECOGNITION_SERVICE_URL:http://localhost:5000}
# Keep temporary face files for debugging (true) or delete them (false)
face.recognition.keep.files=${FACE_RECOGNITION_KEEP_FILES:true}
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
