# Exclude build outputs and IDE files from Docker context
target/
.mvn/
mvnw
mvnw.cmd
.git/
.gitignore
.idea/
*.iml
.vscode/
.settings/
.project
.classpath
*.log
*.tmp

# Environment files should not be included in Docker build context
.env

# Keep src/ and pom.xml as these are needed for build
