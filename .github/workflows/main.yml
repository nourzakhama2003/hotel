# name: Build and Deploy to Production
# on:
#   push:
#     branches:
#       - main
#   workflow_dispatch:  # Allow manual trigger

# jobs:
#   build-and-push:
#     name: Build and Push Docker Images
#     runs-on: ubuntu-latest
#     outputs:
#       image_tag: ${{ steps.set_tag.outputs.tag }}
    
#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v4

#       # ==========================================
#       # BACKEND BUILD
#       # ==========================================
#       - name: Setup JDK 17
#         uses: actions/setup-java@v4
#         with:
#           distribution: "corretto"
#           java-version: "17"
#           cache: 'maven'

#       - name: Build Backend JAR
#         run: |
#           cd hotel-backend
#           mvn clean package -DskipTests
#           echo "✅ Backend JAR built successfully"

#       # ==========================================
#       # FRONTEND BUILD
#       # ==========================================
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#           cache: 'npm'
#           cache-dependency-path: hotel-frontend/package-lock.json

#       # Frontend environment.prod.ts is now committed to Git
#       # No need to create it dynamically

#       - name: Install Frontend Dependencies
#         run: |
#           cd hotel-frontend
#           npm ci --legacy-peer-deps
#           echo "✅ Frontend dependencies installed"

#       # Note: Frontend build happens inside Docker with production configuration

#       # ==========================================
#       # DOCKER IMAGE VERSIONING
#       # ==========================================
#       - name: Set Image Tag
#         id: set_tag
#         run: |
#           # Use git tag if exists, otherwise use short SHA
#           if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
#             TAG="${GITHUB_REF#refs/tags/}"
#           else
#             TAG="$(echo ${GITHUB_SHA} | cut -c1-8)"
#           fi
#           echo "tag=${TAG}" >> $GITHUB_OUTPUT
#           echo "Using image tag: ${TAG}"

#       # ==========================================
#       # DOCKER BUILD & PUSH
#       # ==========================================
#       - name: Login to Docker Hub
#         uses: docker/login-action@v3
#         with:
#           username: ${{ secrets.DOCKERHUB_USERNAME }}
#           password: ${{ secrets.DOCKERHUB_TOKEN }}

#       - name: Delete Old Frontend Image from Docker Hub
#         run: |
#           echo "Attempting to delete old frontend images to ensure fresh rebuild..."
#           # Use docker.io API to delete old tags (requires token)
#           # For now, we'll just log that we're pushing new tags
#           echo "✅ Will push new frontend image to overwrite latest"
#         continue-on-error: true

#       - name: Build and Push Backend Image
#         uses: docker/build-push-action@v5
#         with:
#           context: hotel-backend
#           file: docker/backend/Dockerfile
#           push: true
#           tags: |
#             ${{ secrets.DOCKERHUB_USERNAME }}/hotel-backend:${{ steps.set_tag.outputs.tag }}
#             ${{ secrets.DOCKERHUB_USERNAME }}/hotel-backend:latest

#       - name: Build and Push Frontend Image
#         uses: docker/build-push-action@v5
#         with:
#           context: hotel-frontend
#           file: docker/frontend/Dockerfile
#           build-args: |
#             ENVIRONMENT=production
#           push: true
#           tags: |
#             ${{ secrets.DOCKERHUB_USERNAME }}/front-angular:${{ steps.set_tag.outputs.tag }}
#             ${{ secrets.DOCKERHUB_USERNAME }}/front-angular:latest

#       - name: Images Pushed Successfully
#         run: |
#           echo "✅ Backend image: ${{ secrets.DOCKERHUB_USERNAME }}/hotel-backend:${{ steps.set_tag.outputs.tag }}"
#           echo "✅ Frontend image: ${{ secrets.DOCKERHUB_USERNAME }}/front-angular:${{ steps.set_tag.outputs.tag }}"

#   # ==========================================
#   # DEPLOY TO VPS
#   # ==========================================
#   deploy:
#     name: Deploy to VPS
#     runs-on: ubuntu-latest
#     needs: build-and-push
    
#     steps:
#       - name: Deploy to Production VPS
#         uses: appleboy/ssh-action@v1.0.0
#         with:
#           host: ${{ secrets.VPS_IP }}
#           username: ${{ secrets.VPS_USERNAME }}
#           password: ${{ secrets.VPS_USERNAME_PASSWORD }}
#           script: |
#             echo "🚀 Starting deployment..."
            
#             # Navigate to app directory
#             cd /opt/hotel-app || exit 1
            
#             # Pull latest code (for docker-compose.yml and other config changes)
#             git pull
#             echo "✅ Latest code pulled from GitHub"
            
#             # Create production .env file (matches working VPS config exactly)
#             cat > .env << 'EOF'
#             # -------------------------------------------------
#             # PRODUCTION ENVIRONMENT (VPS)
#             # -------------------------------------------------
#             VERSION=${{ needs.build-and-push.outputs.image_tag }}
#             PROFILE=prod
            
#             # -------------------------------------------------
#             # MySQL (hotel-backend)
#             # -------------------------------------------------
#             MYSQL_ROOT_PASSWORD=${{ secrets.MYSQL_ROOT_PASSWORD }}
#             MYSQL_ROOT_USERNAME=root
#             APP_DB_NAME=hotel
            
#             # Dedicated user for production
#             MYSQL_USER=${{ secrets.MYSQL_USER }}
#             MYSQL_PASSWORD=${{ secrets.MYSQL_PASSWORD }}
            
#             # -------------------------------------------------
#             # PostgreSQL (Keycloak)
#             # -------------------------------------------------
#             KEYCLOAK_DB_NAME=keycloak
#             KEYCLOAK_DB_USER=admin
#             KEYCLOAK_DB_PASSWORD=${{ secrets.KEYCLOAK_DB_PASSWORD }}
            
#             # -------------------------------------------------
#             # Keycloak Admin
#             # -------------------------------------------------
#             KEYCLOAK_ADMIN_USER=admin
#             KEYCLOAK_ADMIN_PASSWORD=${{ secrets.KEYCLOAK_ADMIN_PASSWORD }}
            
#             # -------------------------------------------------
#             # URLs Keycloak (PRODUCTION with HTTPS)
#             # -------------------------------------------------
#             # Internal (Docker network)
#             KEYCLOAK_SERVER_URL=http://keycloak:8080
            
#             # External (via Nginx reverse proxy with SSL)
#             KEYCLOAK_EXTERNAL_FQDN=app.46.lebondeveloppeur.net
#             KEYCLOAK_ISSUER_URI=https://app.46.lebondeveloppeur.net/realms/hotelrealm
#             KEYCLOAK_JWK_SET_URI=https://app.46.lebondeveloppeur.net/realms/hotelrealm/protocol/openid-connect/certs
            
#             # -------------------------------------------------
#             # Stripe
#             # -------------------------------------------------
#             STRIPE_SECRET_KEY=${{ secrets.STRIPE_SECRET_KEY }}
#             STRIPE_PUBLIC_KEY=${{ secrets.STRIPE_PUBLIC_KEY }}
            
#             # -------------------------------------------------
#             # Mail (Gmail)
#             # -------------------------------------------------
#             MAIL_USERNAME=${{ secrets.MAIL_USERNAME }}
#             MAIL_PASSWORD=${{ secrets.MAIL_PASSWORD }}
            
#             # -------------------------------------------------
#             # Keycloak image version
#             # -------------------------------------------------
#             KEYCLOAK_VERSION=25.0.5
#             EOF
            
#             echo "✅ Environment file created"
            
#             # Pull latest images (use 'docker compose' not 'docker-compose')
#             docker compose pull
#             echo "✅ Images pulled from Docker Hub"
            
#             # Restart services
#             docker compose up -d --force-recreate
#             echo "✅ Services restarted"
            
#             # Clean up old images
#             docker image prune -f
#             echo "✅ Old images cleaned"
            
#             # Show running containers
#             docker compose ps
            
#             echo "🎉 Deployment completed successfully!"
